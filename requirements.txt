# ticket-to-code Requirements
# ─────────────────────────────
# Core framework
fastapi>=0.111.0,<1.0.0
uvicorn[standard]>=0.30.0,<1.0.0

# Configuration management
pydantic-settings>=2.3.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0

# GitHub utilities (API calls, workflow dispatch, PR creation)
PyGithub>=2.3.0,<3.0.0

# AI / LLM — DSPy for structured prompting, LiteLLM for model routing
dspy>=2.6.0,<3.0.0
# Pin litellm — 1.82.8 was malicious, 1.83.0 fixes CVE-2026-35030 (CRITICAL)
litellm==1.83.0

# HTTP client (for Jira API calls and general async requests)
httpx>=0.27.0,<1.0.0

# AWS Bedrock support (required when LITELLM_MODEL starts with "bedrock/")
boto3>=1.34.0,<2.0.0

# Pinned transitive deps to resolve CVEs
ujson>=5.12.0          # CVE-2026-32874, CVE-2026-32875
urllib3>=2.6.3         # CVE-2025-66418, CVE-2025-66471, CVE-2026-21441
